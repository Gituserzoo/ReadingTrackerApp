import React from 'react';
import { SafeAreaView } from 'react-native';
import ReadingForm from './screens/ReadingForm';

export default function App() {
  return (
    <SafeAreaView>
      <ReadingForm />
    </SafeAreaView>
  );
}
